// 🅱️ BOOTSTRAP
import Icon from './icon/Icon'

const StarsRating = ({ book }) => {

    // ⚙️ Strapi's API ROUTES :
    const API_URL = process.env.REACT_APP_API_URL;

	return (
        <div></div>
	);
};

export default StarsRating;
